const Button = ({text}) => {
  return (
    <>
      <button className="h-12 rounded-lg  font-bold px-5h-12 rounded-lg font-bold px-5">
       {text}
      </button>

    </>
  )
};

export default Button;
